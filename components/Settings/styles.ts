import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingTop: 100,
    paddingLeft: 20,
  },
  slider: {
    width: 200,
    height: 40,
  },
  sliderContainer: {
    flexDirection: "row",
    alignItems: "center",
  },
});
